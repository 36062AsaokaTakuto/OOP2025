<Window x:Class="CustomerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomerApp" SizeToContent="Height"
        mc:Ignorable="d"
        Title="顧客管理アプリケーション" Width="850">
    <Window.Resources>
        <local:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Column="0" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="名前" FontSize="15" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <TextBox x:Name="NameTextBox" Grid.Row="0" Grid.Column="1" Margin="3"/>
            <Label Grid.Row="1" Grid.Column="0" Content="電話番号" FontSize="15" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <TextBox x:Name="PhoneTextBox" Grid.Row="1" Grid.Column="1" Margin="3"/>
            <Label Grid.Row="2" Grid.Column="0" Content="住所" FontSize="15" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <TextBox x:Name="AddressTextBox" Grid.Row="2" Grid.Column="1" Margin="3"/>
            
            
            
        </Grid>
        <Grid Grid.Column="0" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="4*"/>
            </Grid.RowDefinitions>
            <Button Content="登録" x:Name="SaveButton" Grid.Row="0" VerticalAlignment="Center" Margin="5,0,5,3" Click="SaveButton_Click"/>
            <Button Content="更新" x:Name="UpdateButton" Grid.Row="1"  VerticalAlignment="Center" Margin="5,0,5,3" Click="UpdateButton_Click"/>
            <Button Content="削除" x:Name="DeleteButton" Grid.Row="2" VerticalAlignment="Center" Margin="5,0,5,3" Click="DeleteButton_Click"/>
            <Grid Grid.Column="0" Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>
                <Button Content="画像" x:Name="PictureButton" Grid.Column="0" Width="70" Height="80" Margin="0,0,5,5" Click="PictureButton_Click"/>
                <Image  x:Name="PictureImage" Grid.Column="1" Height="80" Margin="5,0,5,5" Stretch="Uniform"/>
            </Grid>
            
        </Grid>
        <Grid Grid.Column="1" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="4*"/>
            </Grid.RowDefinitions>
            <TextBox x:Name="SearchTextBox" Grid.Row="0" Margin="5" FontSize="20"/>
            <ListView x:Name="CustomerListView" Grid.Row="1" Margin="5" SelectionChanged="CustomerListView_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="3*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Id}" Margin="5"/>
                                <TextBlock Text="{Binding Name}" FontSize="20" Margin="5"/>
                                <TextBlock Text="{Binding Phone}" Foreground="Purple" Margin="5"/>
                                <TextBlock Text="{Binding Address}" Foreground="Green" Margin="5"/>
                            </StackPanel>
                            <Image Grid.Column="1" Width="200" Height="200" Stretch="Uniform" Margin="10"
                                    Source="{Binding Picture, Converter={StaticResource ByteArrayToImageConverter}}"/>
                        </Grid>    
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
       

    </Grid>
    
</Window>
